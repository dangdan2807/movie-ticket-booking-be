version: '3.8'
services:
  mysql:
    container_name: mysql-1
    image: mysql:latest
    ports:
      - "13306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: movie_ticket_booking
      MYSQL_USER: admin
      MYSQL_PASSWORD: 123456
    volumes:
      - ./Database/import.sql:/docker-entrypoint-initdb.d/import.sql
    command: --init-file /docker-entrypoint-initdb.d/import.sql

  seq:
    container_name: seq
    image: datalust/seq:latest
    ports:
      - "5341:80"
    volumes:
      - ./seq-data:/data
    environment:
      - ACCEPT_EULA=Y
      - SEQ_UI=1

  web-api:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    container_name: web-api
    image: dangdan2807/short-url:v1.0
    ports:
      - "13080:80"
      - "13443:443"
    depends_on:
      - mysql
      - seq
    environment:
      - ConnectionStrings__DefaultConnection=Server=mysql;Port=3306;Database=movie_ticket_booking;User=admin;Password=123456
